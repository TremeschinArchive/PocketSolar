\documentclass[journal,a4paper]{IEEEtran}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{float}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,
    citecolor=red,
    urlcolor=cyan,
}
\urlstyle{same}

\usepackage[utf8]{inputenc}
\usepackage{fontspec}
\setmainfont{texgyrepagella}[
    Extension = .otf,
    UprightFont = *-regular,
    BoldFont = *-bold,
    ItalicFont = *-italic,
    BoldItalicFont = *-bolditalic,
]
\mathversion{bold}


\begin{document}
    \title{
        Solar Panel IV Curve Tracker - ViyLine
        \\ {\Large Using Microchip© PIC16F877A}
    }

    \author{et al}
    \markboth{Electrical Engineering | \today}{}
    \maketitle

    \begin{abstract}
        Pronounced \emph{Vee-Aye-Line}, this
    \end{abstract}

    \section{Theory}
        \IEEEPARstart{T}{he} following equation

        $$I = I_L - I_O e^{\frac{qV}{nkT}}$$
        $$y = A - B e^{kx}$$



        $$A - y = B e^{kx}$$
        $$\frac{A - y}{B} = e^{kx}$$
        $$\ln \left( \frac{A - y}{B} \right) = kx$$
        $$\ln(A - y) - \ln(B) = kx$$
        $$\ln(A - y) = kx + \ln(B)$$

        % \ln(A - y) -> ln(A) + ln(1 - y/A)
        $$ = kx + \ln(B)$$



        % Subtrair $A$ dos dados ($\max(y)$)
        % $$A - y = B e^{kx}$$
        % $$\frac{A - y}{B} = e^{kx}$$
        % $$\ln\left( \frac{A - y}{B} \right) = kx$$
        % $$\ln(A - y) - \ln(B) = kx$$
        % $$\ln(A - y) = kx + \ln(B)$$
        % $$y = ax + b$$
        % $$
        % \begin{cases}
        %     a = k \\
        %     b = \ln(B) \\
        % \end{cases}
        % $$


        % $$y = A - B (e^{kx} - 1)$$
        % $$A - y = B (e^{kx} - 1)$$
        % $$\frac{A - y}{B} = e^{kx} - 1$$


\end{document}















